<table class="table table-striped">
    <thead>
        <tr>
            <td>@nameof(ExpenseModel.Date)</td>
            <td>@nameof(ExpenseModel.Category)</td>
            <td>@nameof(ExpenseModel.Amount)</td>
            <td>Update</td>
            <td>Delete</td>
        </tr>
    </thead>
    <tbody>
        @foreach (ExpenseModel expense in Expenses)
        {
            <tr>
                <td>@expense.Date</td>
                <td>@expense?.Category</td>
                <td>@expense.Amount</td>
                <td>
                    <button class="btn btn-primary" @onclick="() => NavigateToUpdateExpensePage(expense.Id)">Update</button>
                </td>
                <td>
                    <button class="btn btn-primary" @onclick="() => DeleteExpenseAsync(expense)">Delete</button>
                </td>
            </tr>
        }
    </tbody>
</table>

@code {
    [Inject]
    private NavigationManager _navigationManager { get; set; }

    [Parameter]
    public IEnumerable<ExpenseModel> Expenses { get; set; }

    [Parameter]
    public EventCallback<ExpenseModel> OnDeleteExpense { get; set; }

    private async Task DeleteExpenseAsync(ExpenseModel expense)
    {
        await OnDeleteExpense.InvokeAsync(expense);
    }

    private void NavigateToUpdateExpensePage(Guid id)
    {
        _navigationManager.NavigateTo($"/expenses/update-expense/{id}");
    }
}
